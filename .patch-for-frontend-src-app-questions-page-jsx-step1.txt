setSelectedAnswers((prev) => ({
      ...prev,
      [questionId]: answer,
    }));
  };
    setShowAnswers(true);
  };
=======  
  const handleAnswerSelect = (questionId, answer) => {
    setSelectedAnswers((prev) => ({
      ...prev,
      [questionId]: answer,
    }));
  };
